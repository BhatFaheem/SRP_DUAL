[General]
network = ned.SecureDualNetwork  # Use the fully qualified name (package + network)
sim-time-limit = 100s
ned-path = .;./ned  # Add this line to search the current directory and "ned" subdirectory

# Enable debug logging
*.**.debug = true